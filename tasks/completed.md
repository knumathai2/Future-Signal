<!--
Purpose:        Archive of completed tasks (accumulate; do not delete)
Owner:          Implementer roles / PM
Update Trigger: Task completed
Harness Version: 1.1
-->

# Completed Tasks — Outlook Signals

_Last updated: 2026-07-11_

| ID | Task | Completed | Owner | Notes |
|----|------|-----------|-------|-------|
| TASK-062 | Serve the v4 context/report API | 2026-07-11 | Backend Implementer | `backend/TASK-062-context-report-api`. Activated the strict v4 runtime response, reconstructing every deterministic field from the linked metric/snapshot and same-episode verified candidates before serving. Public output exposes only approved candidate/source fields and rejects legacy/failed/malformed rows, missing or reordered evidence, non-verified/unknown/episode-mismatched candidates, empty or invalid sources, metric/content tampering, URL/domain mismatch, and invalid timing as `not_yet_generated`; unknown issues remain 404. Static fallback no longer fabricates a report. Sixty-one focused API tests and the full 309-test Backend suite passed with Ruff, diff, OpenAPI, and wording checks clean. No DB write, migration application, provider call, deployment, or production change occurred. |
| TASK-061 | Generate evidence-grounded v4 reports | 2026-07-11 | Data/AI Implementer | `data-ai/TASK-061-evidence-report-v4`. Added a strict seven-field v4 envelope whose metric, context, relationship-boundary, limitations, and caution fields are deterministic; the model may author only the issue overview and later-check field. Stored rows carry metric/candidate evidence references and same-episode verified candidate IDs. Added mismatch, nonexistent evidence, missing-source, unsafe-language, no-context, prior-version, partial-failure, retry, and cumulative-writer-budget coverage. Focused 121 tests and the full 298-test Backend suite passed with Ruff and diff checks clean. No provider call, migration application, or configured DB write occurred. |
| TASK-060 | Connect context research to the scheduled batch | 2026-07-11 | Data/AI Implementer | `data-ai/TASK-060-context-batch`. Added signal/change/heat/staleness/backfill selection, per-market research→verification→append-only storage, verified-only downstream IDs, three-candidate public cap, no-candidate state, duplicate idempotency, failure isolation, provider-usage audit, and pre-call USD 100 budget reservation. Connected the stage between signals and reports with optional configuration/backfill CLI controls. Twelve batch and seven scheduled tests passed; full Backend suite reached 280 tests with Ruff/diff checks clean. No provider call, migration application, or configured DB write occurred. |
| TASK-059 | Build deterministic and independent-AI verification | 2026-07-11 | Data/AI Implementer | `data-ai/TASK-059-context-verification`. Added deterministic URL/date/entity/condition/source-independence gates, official-single and independent-multi-source paths, republish detection, unsafe relationship/result rejection, evidence hashing, and a no-web verifier using a different provider family. Model approval cannot promote hard-gate failures; verifier disagreements and new claims are withheld. Thirty verification tests plus 22 research tests passed; full Backend suite reached 266 tests with Ruff and diff checks clean. No provider call or DB write occurred. |
| TASK-058 | Build OpenRouter web-research client | 2026-07-11 | Data/AI Implementer | `data-ai/TASK-058-context-research`. Added bounded `openrouter:web_search` Chat Completions research using the existing OpenAI SDK, deterministic metadata query allowlists, strict provider JSON parsing, annotation-only citation normalization, exact URL-to-citation-ID mapping, model-body URL rejection, provider usage capture, and settings clamped to six searches/30 results. Added OpenRouter example variables without secrets. Twenty-one fake-client tests, full 235-test Backend suite, Ruff, and `git diff --check` passed. No provider call or DB write occurred. |
| TASK-057 | Add automated-context storage schema | 2026-07-11 | Backend Implementer | `backend/TASK-057-context-schema`. Added append-only `002_context_candidates.sql`, `ContextCandidate`, and `ContextCollectionRun` with constrained states, episode-scoped evidence uniqueness, lookup indexes, secret-free JSON fields, and documented cascade behavior. Added 14 focused tests covering test/Postgres DDL, states, duplicate handling, counts, indexes, fields, migration immutability, and parent deletion. Focused tests, full 214-test Backend suite, Ruff, and `git diff --check` passed. Migration was not applied to any database. |
| TASK-056 | Approve automated-context policy and v4 contract | 2026-07-11 | PM / Planner | `pm/TASK-056-auto-context-policy`. ADR-038 records explicit human approval for TASK-056~065, verified-only automated context, the strict seven-field v4 response, append-only migration, OpenRouter research/verification/writing up to USD 100 cumulative usage, and local/development DB writes. Updated the constitution, PRD, Service/Technical/UX Design, API contract, glossary, standards, roadmap, architecture, and task ledgers. Deployment, production DB writes, unrelated API/schema work, infrastructure changes, and new dependencies remain excluded. Documentation cross-reference scans and `git diff --check` passed; no provider call or DB write occurred. |
| ISS-011 | Restore the TASK-054 Frontend build under the ES2020 TypeScript target | 2026-07-10 | Frontend Implementer / PM | Fixed in PR #53 at the user's direction. `FeaturedIssueCard.tsx` used `Array.prototype.at()`, whose type is unavailable under the existing `ES2020` library. Replaced both last-item reads with equivalent index access without changing configuration, dependencies, UI copy, or runtime behavior. Frontend typecheck, lint, report-parser regression, production build, and changed-file Prettier check passed; Backend verification remained at 200 passing tests with Ruff clean. |
| TASK-055 | Analyze and task-plan the connection between context candidates and AI summaries | 2026-07-10 | PM / Planner | `pm/TASK-055-context-summary-strategy`. Reviewed PRD, Service/Technical/UX Design, ADR-032~035, the v3 API/prompt/assembly/UI path, candidate seed flow, and read-only local API outputs. Recorded the disconnection and change-episode/evidence-bundle model in `reports/task-055-context-candidate-ai-summary-strategy.md`; decomposed a one-person 20-hour core program into `TASK-056`~`TASK-065` in `reports/task-055-automated-context-execution-plan.md`; and added a prioritized 25-hour quality, provenance, history, multilingual, reliability, and cross-issue stretch sequence as `TASK-066`~`TASK-074` in `reports/task-055-automated-context-stretch-plan.md` and `tasks/backlog.md`. No product code, schema, API, policy, dependency, infrastructure, deployment, external provider call, or database write was changed or performed. |
| TASK-054 | Dashboard, issue list, and detail information-architecture redesign | 2026-07-10 | Frontend Implementer | `frontend/TASK-054-information-architecture`. Reopened the completed task and realigned Home to the approved structure: 7-day-only Home default, desktop single-line header/mobile icon refresh, two-column introduction plus actual rank-1 featured issue, real-history Recharts preview, upward/downward/unchanged/insufficient summary, featured-inclusive top-five desktop table/mobile cards, exact Home search/category URLs, and per-category valid-change arithmetic means. Retained existing `/issues` 24-hour default, routes, detail/report flows, APIs, schema, dependencies, Vercel configuration, footer, and caution/timing policy. Verified required npm commands, Prettier, diff/copy lint, actual API totals/means, -19.5pp downward history consistency, interactions, 44px/accessibility, state handling, and 320/390/768/1024/1280px no-overflow Browser QA. No deployment or backend/DB write. |
| ISS-010 | Restore scheduled GitHub Actions batch configuration and report generation | 2026-07-10 | Debugger / Data-AI Implementer | `debug/ISS-010-actions-secrets`, draft PR #51. Restored the repository Actions `DATABASE_URL` and approved AI credential without exposing values, aligned the `OPENAI_MODEL` repository variable with the approved local project setting, and tightened the v3 prompt with the existing ADR-033 character bounds, approved Korean source compound, and current-reading scope. GitHub Actions run `29073226485` succeeded: 50 processed, 0 failed, 10 reports succeeded, 0 skipped. Local verification: 200 Backend tests, Ruff, `git diff --check`, and read-only validation of the latest 10 v3 rows all passed structural, wording-safety, and semantic checks. No schema, dependency, public API, safety-policy, or deployment change. |
| TASK-053 | Review v3 integration copy and contract | 2026-07-10 | Reviewer | `review/TASK-053-v3-report-copy-lint`. Integrated the merged TASK-049/TASK-051 heads with TASK-050, which PR #47 had merged into the frontend branch instead of `main`. Verified the ADR-033 eight-field schema, nullability, labels, order, version gating, data-as-of timing, caution placement, and 320px/375px layout. Closed gaps in the five-sentence limit, public-participant-data and conditional-data semantic validation, deterministic fallback tests, Frontend parser coverage, and Korean hard-block copy. Verification: 198 Backend tests, Ruff, Frontend typecheck/lint/build/parser checks, `git diff --check`, copy lint, and Browser QA passed. No provider call, database write, migration, dependency, infrastructure change, or deployment was performed. |
| TASK-051 | Implement v3 dynamic report UI | 2026-07-10 | Frontend Implementer | PR #48 merged the ADR-033 parser and one-section-at-a-time report card. TASK-053 added final parser sentence-limit/order/null-context regressions, aligned notice copy, and verified 7-section null-context plus 8-section non-null-context layouts at 320px and 375px. |
| TASK-050 | Implement v3 report API/read contract | 2026-07-10 | Backend Implementer | PR #47 implemented the strict v3 Pydantic/read contract and version gating. Because the PR was merged into the frontend task branch rather than `main`, TASK-053 integrated its head with current `origin/main`, resolved the generator/schema conflicts, added the five-sentence read-time contract check, and reran the complete integration suite. |
| TASK-049 | Implement v3 report generation content | 2026-07-10 | Data/AI Implementer | PR #49 merged the v3 generator, deterministic caution/limitations/context fields, prompt-version update, and semantic safety checks. TASK-053 added the ADR-033 five-sentence guard plus required public-participant-data and conditional-public-data scope checks. |
| TASK-052 | Day 5 v3 implementation allocation | 2026-07-10 | PM | `pm/TASK-052-v3-implementation-allocation`. Fetched and confirmed latest `origin/main` at `106af52`, including PR #45 (`TASK-047`) and PR #46 (`TASK-048`). Created the Day 5 v3 implementation allocation in `reports/day-5-v3-implementation-allocation.md`, opened active implementation/review assignments for `TASK-049`, `TASK-050`, `TASK-051`, and `TASK-053`, and updated project/session status records. No runtime code, schema migration, dependency, infrastructure, deployment, provider call, or database write was changed or performed. |
| TASK-048 | Design the v3 Report Contract | 2026-07-10 | Backend Implementer | `backend/TASK-048-v3-report-contract`. Drafted and received user/PM approval for the ADR-033 eight-field v3 contract, including ADR-032 mapping, exact types/nullability/Unicode character bounds, a maximum of five concise sentences per field, external-context Option A, conditional and non-causal safety rules, the four-level Korean caution matrix, evidence-first Frontend labels/order, and a strict Pydantic v2 model snippet. Data/AI and Frontend completed read-only reviews. Runtime API, generator, schema, tests, Frontend types/UI, database, migrations, dependencies, infrastructure, and deployment remain unchanged pending coordinated implementation. |
| — | AI Development Harness v1.1 initial setup | 2026-07-07 | PM | Standard tier, monorepo, npm+pip, GitHub Actions |
| TASK-001 | Repo scaffold: `/frontend` + `/backend` project shells | 2026-07-08 | Backend Implementer | `backend/TASK-001-repo-scaffold`. FastAPI app import-smoke-tested; frontend hand-scaffolded (npm/node unavailable in this environment, not run). |
| TASK-002 | DB schema draft for MVP tables | 2026-07-08 | Backend Implementer | Draft accepted as a Day 1 artifact in ADR-011. Migration remains unapplied; applying schema changes to any shared or production database still requires separate human approval. |
| TASK-003 | API contract draft and response-shape agreement | 2026-07-08 | Backend Implementer + PM | PM/Frontend sign-off recorded. Public route names and response fields are accepted for Day 2 implementation dependency; ADR-008 accepts `200 {"status": "not_yet_generated"}` for the no-report-yet response. |
| TASK-011 | Add `GET /api/health` endpoint | 2026-07-08 | Backend Implementer | `backend/TASK-011-health-endpoint`. Verified via pytest + a live uvicorn request; appears in `/openapi.json`. |
| TASK-004 | Polymarket Gamma/CLOB live spike | 2026-07-08 | Data/AI Implementer | Gamma fields, pagination, rate-limits, CLOB history shape documented; samples saved |
| TASK-005 | Wireframe dashboard/detail screens; start UI against dummy JSON | 2026-07-08 | Frontend Implementer | React/Vite/Tailwind/Recharts UI implemented with dashboard, issue cards, detail view, caution badges, data-as-of timestamps, dummy data contract, and loading/empty/error states. |
| TASK-006 | Finalize MVP scope doc, prohibited-wording policy, and Day 1 presentation story | 2026-07-08 | PM | P0 scope remains locked, wording policy references `standards.md` and `memory/glossary.md`, Day 1 implementation status recorded in `reports/day-1-implementation-status.md`, and demo message stays in issue-monitoring framing. |
| TASK-031 | Organize Day 2 user scenarios, judging Q&A, and scope guardrails | 2026-07-08 | PM | Day 2 active work assigned to `TASK-007`, `TASK-008`, `TASK-009`, `TASK-010`, and `TASK-012`; sequencing, PM scenario seed, and judging Q&A seed recorded in `reports/day-2-work-allocation.md`. |
| TASK-007 | Batch collector: fetch + normalize (steps 1-2) | 2026-07-08 | Data/AI Implementer | PR #9 merged. Final `normalized_samples.json` contains 50 schema-shaped records with required top-level fields and display-safe descriptions; `skipped_records.json` records structured skip reasons. Backend collector contract tests pass, and ADR-014 records the raw-description boundary. |
| TASK-012 | Home dashboard UI (ranked issue cards) | 2026-07-08 | Frontend Implementer | Integrated categories and issues with backend routes, added UI filters/sort options, and implemented error-to-stale-dummy fallback. |
| TASK-033 | Korean default UI copy pass | 2026-07-08 | Frontend Implementer | `frontend/TASK-033-korean-default-ui`. Switched static frontend UI copy, HTML lang, date/time formatting, caution copy, template summary text, fallback/demo issue copy, and category display labels to Korean without adding dependencies or changing API/backend/schema/deployment surfaces. Verified `typecheck`, `lint`, `build`, content-safety scans, and dashboard desktop/mobile overflow checks. |
| TASK-008 | Batch collector: snapshot + metrics (steps 3-5) | 2026-07-08 | Data/AI Implementer | `data-ai/TASK-008-snapshot-metrics`. `app/core/snapshot_metrics.py` implements Technical Design §6 steps 3-5 only (no signal detection, collection logs, or AI trigger). Verified end-to-end against the real 50-record `normalized_samples.json` from `TASK-007` via an in-memory SQLite local/dev-safe path (`tests/test_snapshot_metrics.py`, 20 tests) - no shared/production database was written to, per the AGENTS.md approval gate. New markets correctly fall back to `insufficient_data` instead of a computed delta; `market_snapshots`/`market_metrics` batch inserts retry once then log-and-skip a failing market without crashing the run, and parent `markets`/`market_outcomes` rows are committed before snapshot fallback can roll back a failed batch. See ADR-015 for two scope judgment calls: bootstrapping `markets`/`market_outcomes` rows (necessary FK plumbing TASK-007 never does) and shipping only the P0 metric subset (`change_24h`, `change_7d`, a placeholder `heat_score`, and `sufficient`/`insufficient_data` confidence levels only - `sufficient` now requires both MVP windows to be computable; `caution_low_activity`/`caution_high_volatility`/`volatility_score`/`attention_score` deferred, tracked as TD-008). Backend Implementer should re-verify against a real Postgres once `TASK-002`'s migration is approved for a shared/dev database. |
| TASK-009 | Expectation-shift signal detection (±5pp threshold) | 2026-07-08 | Data/AI Implementer | `data-ai/TASK-009-signal-detection`. `app/core/signal_detection.py` implements Technical Design §8 (batch step 6) only - MVP scope is `expectation_shift`/`medium` severity, no `attention_spike`/`unusual_activity`/other severity tiers. `EXPECTATION_SHIFT_THRESHOLD = 0.05` (±5pp, on the same 0-1 scale as `market_metrics.change_24h`) and a 24h cooldown (reusing TASK-008's `WINDOW_24H`) are named constants, not magic numbers. `detect_signals_for_run(db, run_timestamp)` re-queries `market_metrics` by `computed_at` rather than modifying TASK-008's `run_snapshot_and_metrics` return type (ADR-016) - composes as `detect_signals_for_run(db, run_snapshot_and_metrics(...).run_timestamp)`. Markets with `confidence_level = insufficient_data` or a null `change_24h` are never evaluated (no fabricated signals); `issue_signals` rows are insert-only, never updated. Verified via `tests/test_signal_detection.py` (14 tests) against an in-memory SQLite local/dev-safe path - covers below-threshold (no signal), at/above-threshold (fires with correct `magnitude`/`severity`/`window`/`detail`), and no-duplicate-within-cooldown/fires-again-after-cooldown-expires. No schema change, new dependency, or shared/production database write was made. `issue_signals` rows are now queryable for whatever consumes batch step 8 (AI report regeneration trigger) - not implemented here, per task scope. |
| TASK-010 | Core read API endpoints | 2026-07-08 | Backend Implementer | PR #10 merged and approved after follow-up. `/api/issues`, `/api/issues/{id}`, and `/api/issues/{id}/history` read latest available snapshot/metric/history data when configured and degrade to documented static fallback with honest `data_as_of` when live data is unavailable. Backend tests cover live, fallback, invalid-param, unknown-id, and auxiliary-query failure behavior. |
| TASK-034 | Day 3 work allocation and scope guardrails | 2026-07-09 | PM | Opened Day 3 active work for `TASK-013`, `TASK-014`, `TASK-017`, `TASK-035`, and `TASK-036`; sequencing and guardrails recorded in `reports/day-3-work-allocation.md`. `TASK-015` remains deferred until the detail/chart/badge path is stable. |
| TASK-035 | Issue detail/history API readiness pass | 2026-07-09 | Backend Implementer | `backend/TASK-035-detail-history-readiness`. Verified the merged `TASK-010` `/api/issues/{id}` and `/api/issues/{id}/history` contracts already provide everything the Day 3 chart/tooltip/marker path needs (signals, related_events, core metric fields, window-filtered/ordered history points) - no response-shape change made or requested. Closed two real fallback-path test gaps: `load_live_issues` itself raising `SQLAlchemyError` (as opposed to the already-covered auxiliary-query failures) had zero coverage across list/detail/history, and the history route's unknown-id 404 had never been tested in either live or static-fallback mode. Added `test_history_multiple_snapshots_filtered_and_sorted_by_window`, the first test to seed multiple snapshots per market and assert real 24h/7d/30d window-boundary filtering and ascending order (prior tests only ever had one snapshot). 62/62 backend tests pass (56 baseline + 6 new), ruff clean, only test files changed. Two remaining risks flagged in `memory/session.md` for `TASK-013`/`TASK-036`: the frontend's local adjacent-point marker diff can disagree with the backend's actual windowed `expectation_shift` signal, and `confidence_level` has no query-layer validation against its 4-value enum. |
| TASK-013 | Issue detail UI + Recharts line chart | 2026-07-09 | Frontend Implementer | `frontend/TASK-013-detail-chart`. Hardened the detail chart flow without changing API shapes or dependencies: 24h/7d/30d windows now require baseline-covered history, 30d change no longer falls back to 7d, tooltip text includes timestamp/value/previous-point pp change, markers use API `signals` when present and local adjacent 5pp detection otherwise, and insufficient-history states retain nearby data-as-of timing and caution context. Verified `npm run typecheck`, `npm run lint`, `npm run build`, frontend content-safety scans, local API-fallback browser smoke, and static fallback helper coverage. |
| TASK-036 | Caution-badge logic and expectation-shift marker handoff | 2026-07-09 | Data/AI Implementer | Added logic and thresholds for `caution_low_activity` and `caution_high_volatility` in `compute_confidence_level`. Documented 5pp expectation_shift marker consumption contract in ADR-019. Resolves MVP path for TD-008. |
| TASK-014 | Interpretation-caution badge alignment | 2026-07-09 | Frontend Implementer | `frontend/TASK-014-caution-badges`. Aligned reusable caution badge labels, detail copy, accessibility labels, neutral visual variants, and summary/detail placement for `sufficient`, `caution_low_activity`, `caution_high_volatility`, and `insufficient_data`. Dashboard cards and weekly rows keep badges near issue metrics; detail header, metric caution panel, chart context, and summary card now all surface the issue caution level with nearby data-as-of timing. Verified `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check`, frontend prohibited-wording scan (only code/CSS false positives), browser QA for all four caution states in static fallback mode, desktop detail placement, mobile dashboard/detail overflow, and browser console errors. No dependency, public API, schema, infrastructure, deployment, or wording-policy change was made. |
| TASK-017 | Disclaimer copy, footer, and dedicated notice surface | 2026-07-09 | Frontend Implementer + PM | `frontend/TASK-017-disclaimer-copy`. Added shared brief caution copy and a reusable global footer, wired dashboard/detail footers to a dedicated in-app information notice surface, and kept the notice flow inside local React screen state with no routing dependency, accounts, notifications, public API, schema, infrastructure, deployment, or wording-policy change. Dashboard and detail now link to the full notice surface; detail data-heavy areas keep issue-specific caution state and data-as-of timing near the metric/chart/summary flow. |
| TASK-037 | Day 3 closeout verification and Day 4 handoff | 2026-07-09 | PM | `pm/TASK-037-day-3-closeout`. Verified latest `origin/main` at `89dc3e5` includes all Day 3 P0 work, created `reports/day-3-closeout-plan.md`, closed the Day 3 roadmap milestone, aligned project/session/architecture/issue ledgers, and left Day 4 to start from `TASK-015`, `TASK-016`, `TASK-018`, and `TASK-019`. |
| TASK-038 | Day 4 work allocation and scope guardrails | 2026-07-09 | PM | `pm/TASK-038-day-4-allocation`. Fetched latest git refs and allocated Day 4 active work from `origin/main` at `af83f7e`: `TASK-015`, `TASK-039`, `TASK-016`, `TASK-019`, `TASK-040`, and `TASK-018`. Recorded sequencing, approval gates, and P1/P2 deferrals in `reports/day-4-work-allocation.md`. |
| TASK-015 | Template report generation function and safety filter | 2026-07-09 | Data/AI Implementer | `data-ai/TASK-015-template-report-generation`. Implemented `app/core/ai_report.py` (fixed §10.1/§10.2 system+user prompt templates with zero free-text insertion points beyond named slots, strict 5-field JSON schema parse via `ReportContent` with `extra="forbid"`, and the banned-phrase/pattern safety filter combining standards.md's Content Safety Lint, UX Design §5.3, and memory/glossary.md's wording policy) and `app/core/ai_report_batch.py` (regeneration eligibility - new signal this run / no successful report yet / latest successful report >24h stale - capped to top 10 by `heat_score`, exactly-one-retry on LLM call or malformed-schema failure then `status=failed`, and outright discard-without-retry on a safety-filter failure, in every failure case leaving the previous successful `ai_reports` row as the one served). Per ADR-022, the user explicitly overrode `reports/day-4-work-allocation.md`'s deterministic-template default and approved OpenAI as the real provider (`OpenAIReportClient`, `openai==2.44.0` added to `backend/requirements.txt`, `OPENAI_API_KEY`/`OPENAI_MODEL` added to `app/core/config.py`/`.env.example`) - no key is present in this environment, so no real/billed API call has been made; 100 backend tests pass (62 baseline + 38 new, in `tests/test_ai_report.py` and `tests/test_ai_report_batch.py`), all against a fake `LLMClient`, including a literal round-trip of Technical Design §10.3's reference example through the parser and filter. `ruff check .` clean. Does not touch `app/api/routes/issues.py` - wiring `ai_reports` to the live `GET /api/issues/{id}/report` response is `TASK-039`'s scope, not this task's. |
| TASK-039 | Report API read-path and fallback readiness | 2026-07-09 | Backend Implementer | `backend/TASK-039-stabilize-api-fallback` PR #29 follow-up. Merged latest `origin/main`, preserved the Day 4 task ledger, kept `/api/issues/{id}/history` honest by returning empty `points` when history is unavailable, and wired `/api/issues/{id}/report` to latest successful `ai_reports` rows in live mode. The accepted `not_yet_generated` response remains for absent/failed report reads, unknown IDs return 404, and the static fallback sample behavior is preserved. No public API shape, schema, dependency, infrastructure, deployment, or shared/prod database write was introduced. |
| TASK-016 | Template report display UI | 2026-07-09 | Frontend Implementer | `frontend/TASK-016-report-display-ui`. Added frontend types for the accepted `/api/issues/{id}/report` success and `not_yet_generated` responses, fetched issue detail, 30d history, and report data in the detail flow, and kept report failures isolated to the summary card. The detail summary now renders stored report sections, neutral no-report and fetch-failure states, a compact loading state, and an honest local template-derived fallback only when the report fetch fails. Data-as-of timing uses `report.data_as_of` for successful reports and `issue.dataAsOf` for empty/error states, with the interpretation-caution badge and short caution notice kept next to the report content. Verification: `npm run typecheck`, `npm run lint`, `npm run build`, browser smoke for Home -> Detail -> Chart -> Summary, success/not-yet-generated/report-failure states, and changed-string safety scan. |
| TASK-019 | Curated related-event candidates for representative issues | 2026-07-09 | Data/AI Implementer + PM | `data-ai/TASK-019-curated-events`. Created Python seed script `backend/app/db/seed_related_events.py` curating events for exactly 4 representative issues from `normalized_samples.json` so seeded rows can attach to DB-backed issue detail reads. Added unit tests `tests/test_seed_related_events.py` verifying environmental safety, schema boundaries, normalized/live-reachable IDs, disclaimers, no causal verbs, and no banned terms. Verified through SQLite-backed unit tests. |
| TASK-041 | AI report generation readiness for live demo data | 2026-07-09 | Data/AI Implementer | `data-ai/TASK-041-report-generation-readiness`. Updated `build_prompt_inputs_for_market()` so report generation uses the latest snapshot with `captured_at <= market_metrics.computed_at`, matching `historical_seed` metric timestamps while preserving the skip path when no usable prior snapshot exists. Added SQLite-backed tests for the `+1 microsecond` historical-seed case, future-only snapshot rejection, and `run_ai_report_batch` inserting a `status=success` row with a fake `LLMClient`. Added approved-only local/demo run notes in `reports/task-041-report-generation-readiness.md`. Verified `ruff check .`, focused `pytest tests/test_ai_report_batch.py` (17 passed), and full backend `DATABASE_URL= pytest` (117 passed). Plain `pytest` was also attempted and failed only because the local environment loaded a DB URL requiring unavailable `psycopg2`; no real LLM call or shared/dev DB write was performed. |
| TASK-042 | Combined scheduled batch and manual AI report generation | 2026-07-09 | Data/AI Implementer | `data-ai/TASK-041-report-generation-readiness` follow-up. Added `backend/app/core/scheduled_batch.py` to run data collection or normalized input -> snapshot/metric generation -> expectation-shift signal detection -> AI report generation -> collection logging in one command. Added `--reports-only` for development/demo report generation from the latest existing metric run, documented commands in `backend/README.md`, and added `.github/workflows/daily-batch.yml` to run the combined batch every 24h. Added `backend/tests/test_scheduled_batch.py` for the connected pipeline and reports-only path. Verification: `ruff check app/core tests/test_scheduled_batch.py tests/test_ai_report_batch.py`, focused scheduled/batch tests (54 passed), and full backend `DATABASE_URL= .venv/bin/pytest` (120 passed). A live reports-only run reached OpenAI but failed with `401 Unauthorized`, leaving `ai_reports_success=0`; tracked as `ISS-006`. |
| TASK-043 | AI report output structure: issue explainer scenarios | 2026-07-09 | Data/AI Implementer | `data-ai/TASK-043-issue-explainer-report`. User approved changing `/api/issues/{id}/report` `content` from the original numeric 5-section summary to an 8-slot issue explainer (`issue_explainer`, `why_it_matters`, `current_reading`, `scenario_major_change`, `scenario_limited_change`, `scenario_status_quo`, `check_points`, `caution_note`). `PROMPT_VERSION` advanced to `v2`; backend/frontend schemas, report card labels, API contract, Technical Design §10, ADR-028, and working memory were updated. Legacy v1 stored successful report content now returns `not_yet_generated` instead of a mismatched shape until v2 reports are generated, and legacy `prompt_version` rows are regeneration-eligible even when not yet stale. Verified ruff, focused backend tests (42 passed), full backend tests (130 passed), frontend typecheck/lint/build, `git diff --check`, and changed-string safety scans. No DB schema change, dependency change, deployment, infrastructure change, or DB write was performed. |
| TASK-040 | Day 4 demo script and deck draft | 2026-07-09 | PM | `pm/TASK-040-demo-script-deck-draft`. Created `reports/task-040-demo-script-deck-draft.md` with a 10-slide deck outline, 3-5 minute demo script following Home -> Detail -> Chart -> Summary -> caution notice -> manual context candidate, fallback narration for live/local data, report, chart-history, and context-candidate gaps, Day 5 screenshot/rehearsal checklist, and judge Q&A draft. Also created `reports/task-040-demo-script-deck-draft-prompt.md` as the reusable task-start prompt. Basic hard-block and use-carefully wording scans passed for the new TASK-040 materials and session note; comprehensive cross-surface lint remains `TASK-018`. |
| TASK-044 | Korean issue display titles for dashboard/detail | 2026-07-09 | Frontend Implementer | `frontend/TASK-044-korean-issue-titles`. Added frontend display fields and a deterministic `issueDisplay` mapper so raw English Polymarket titles render as Korean topic labels, issue display names, and one-line 기준 조건 on dashboard cards and detail headers. Raw source titles are preserved only as detail-screen provenance. Static fallback/dummy issues now also populate display subtitles. Verified frontend `typecheck`, `lint`, `build`, browser smoke for dashboard and detail text, changed-string safety scans, and `git diff --check`. No API, schema, dependency, infrastructure, deployment, or DB change was made. |
| TASK-018 | Copy/wording lint pass across user-facing surfaces | 2026-07-09 | PM | `pm/TASK-018-copy-lint`. Ran final Day 4 content-safety lint across frontend UI/fallback data, backend API/report strings, AI report templates and safety filter, related-event candidates, targeted tests, and Day 4 demo/report docs. Resolved prompt-template wording, added a data-as-of timestamp to dashboard weekly rows, cleaned a TASK-044 report wording hit, and recorded the pass in `reports/task-018-copy-lint.md`. Verified frontend `typecheck`, `lint`, `build`, backend `ruff check app tests`, and targeted backend tests (68 passed). Verdict: Pass with notes; no Day 4 closeout blockers remain. |
| TASK-045 | Day 4 closeout verification and Day 5 handoff | 2026-07-10 | PM | `pm/TASK-045-day-4-closeout`. Verified latest `origin/main` includes PR #42 (`TASK-018`), no active Day 4 tasks remain, Day 4 acceptance criteria are satisfied, and closeout evidence is recorded in `reports/day-4-closeout-plan.md`. |
| TASK-047 | v3 AI report policy and scope-lock approval | 2026-07-10 | PM | `pm/TASK-047-v3-ai-report-policy`. Created ADR-032 as the prerequisite approval for v3 AI summary/report policy, the exact v3 report field list, limited `/api/issues/{id}/report` response-shape changes, tightened English/Korean wording-safety criteria, manual-only context-candidate scope, and maintained prohibitions. No product code, schema migration, dependency, infrastructure, deployment, paid external API call, or database write was performed. |

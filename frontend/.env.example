# Leave empty for local development so Vite proxies /api to localhost:8000.
# For an approved split-origin deployment, set only the Backend origin, without
# a path, query, fragment, or trailing slash (for example https://api.example.com).
VITE_API_BASE_URL=
